/**
 * Executable regression and golden-test tools for the revision-377 client.
 *
 * <p>These tests deliberately avoid a separate test framework so architecture,
 * protocol, layout, widget-content, and renderer gates can be compiled and run
 * with the same Java 17 toolchain as the client source.</p>
 */
package rs2.tools;
