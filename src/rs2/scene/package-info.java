/**
 * Contains the local scene graph, map-region decoder, and scene-level world
 * objects used to construct and render the loaded revision-377 map area. Per-tile
 * occupant structures live in {@code rs2.scene.tile}.
 */
package rs2.scene;
